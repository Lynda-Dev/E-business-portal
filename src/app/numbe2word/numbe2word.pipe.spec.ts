import { Numbe2wordPipe } from './numbe2word.pipe';

describe('Numbe2wordPipe', () => {
  it('create an instance', () => {
    const pipe = new Numbe2wordPipe();
    expect(pipe).toBeTruthy();
  });
});
